<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_1">
        <activity android:name=".ActivityGoodsAmount"></activity>
        <activity
            android:name=".ActivityLoadingScreen"
            android:theme="@style/LoadingScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActivityMainLenta"
            android:theme="@style/NoNavbar" />
        <activity
            android:name=".ActivitySignUpMenu"
            android:theme="@style/NoNavbar" />
        <activity
            android:name=".ActivityLogInMenu"
            android:theme="@style/NoNavbar" />
    </application>

</manifest>